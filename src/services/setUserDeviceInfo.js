import database from '@react-native-firebase/database';

export default function setUserDeviceInfo({
    hasNotificationPermission,
    isEmailSubscribed,
    isPushDisabled,
    isSMSSubscribed,
    isSubscribed,
    notificationPermissionStatus,
    userId,
}) {
}